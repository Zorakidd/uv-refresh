name: Release

on:
  workflow_dispatch:
    inputs:
      bump:
        description: "Version bump"
        required: true
        default: patch
        type: choice
        options:
          - patch
          - minor
          - major

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - uses: astral-sh/setup-uv@v10.0.1

      - name: Test before releasing
        run: |
          uv run ruff check .
          uv run pytest

      - name: Bump version
        id: bump
        run: |
          uv version --bump ${{ inputs.bump }}
          echo "version=$(uv version --short)" >> "$GITHUB_OUTPUT"

      - name: Commit, tag, push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add pyproject.toml uv.lock
          git commit -m "Release v${{ steps.bump.outputs.version }}"
          git tag "v${{ steps.bump.outputs.version }}"
          git push origin HEAD --tags

      - name: Create GitHub release
        # A release created with the default GITHUB_TOKEN does not trigger
        # other workflows (GitHub's anti-recursion rule) -- publish.yml's
        # "on: release: published" would never fire, so v0.1.1/v0.1.2 never
        # reached PyPI. RELEASE_TOKEN must be a PAT (fine-grained, "Contents:
        # write" on this repo) stored as a repository secret.
        env:
          GITHUB_TOKEN: ${{ secrets.RELEASE_TOKEN }}
        run: gh release create "v${{ steps.bump.outputs.version }}" --generate-notes
